<%= form_for(@result_type) do |f| %>
  <% if @result_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@result_type.errors.count, "error") %> prohibited this result_type from being saved:</h2>

      <ul>
      <% @result_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

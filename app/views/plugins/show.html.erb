<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<div class='bs_container'>
    <div class="bs_title">
        <b><%= raw @info_content['title'] %></b>
    </div>
    <div class="bs_path">
        <p><%= raw @info_content['path'][0] %></p>
    </div>
    <div class="bs_desc">
        <%= raw @info_content['description'] %>
        <p><%= @info_content['meta']['version'] %></p>
    </div>

</div>
<%#= link_to 'Edit', edit_plugin_path(@plugin) %> 
<%#= link_to 'Back', plugins_path %> 
<%#= link_to 'New request', new_request_path(:plugin_id => @plugin.id) %>
